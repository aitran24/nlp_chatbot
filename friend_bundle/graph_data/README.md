# Graph Data

Nguồn dữ liệu dùng để nạp Neo4j:

- `csv_01a/`: danh sách thi đã chuẩn hóa sang CSV
- `excel/`: file Excel đính kèm thông báo

Script import:

```powershell
.\.venv\Scripts\python.exe scripts\import_neo4j.py --reset
```

Notebook gốc tham chiếu:

- `notebooks/xulicsv.ipynb`
